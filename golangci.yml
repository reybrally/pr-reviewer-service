run:
  timeout: 3m
  tests: true

linters:
  enable:
    - govet
    - errcheck
    - gofmt
    - goimports
    - gosimple
    - staticcheck
    - ineffassign
    - unused
    - misspell

issues:
  exclude-rules:
    - path: _test\.go
      linters:
        - errcheck